server:
  port: 8082

spring:
  application:
    name: user
  datasource:
    username: 'musu'
    url: jdbc:postgresql://localhost:5432/user
    password: 'password'
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format-sql: true
    hibernate:
      ddl-auto: update
    show-sql: true